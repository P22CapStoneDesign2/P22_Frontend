{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Chrome — Vite (5174)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5174",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "Edge — Vite (5174)",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:5174",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
